import { cn } from "@/lib/utils";
import * as React from "react";

const UnMuteIcon = ({
  width = 69,
  height = 69,
  className,
}: {
  width?: number;
  height?: number;
  className?: string;
}) => <div></div>;

export default UnMuteIcon;
